# Supabase client configuration (for development)
# Copy to `.env` and fill with your project's values

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...your-anon-key...

# Optional server-side service role key (DO NOT commit to repo)
# SUPABASE_SERVICE_KEY=your-service-role-key

# Example: Vite will expose variables that start with VITE_.
# When deploying, set these env vars in your hosting provider (Vercel/Netlify/Cloudflare).
